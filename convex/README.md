# Convex Backend

This directory contains the Convex backend functions.

To complete setup:
1. Run `npx convex dev` to connect to a Convex project
2. Follow the prompts to create or select a project
3. Add `VITE_CONVEX_URL` to your `.env.local` file
